# Agent configuration
# Override with flags: agent --model X --permissions auto "prompt"
# Use profile: agent -P research "prompt"

defaults:
  model: claude-opus-4-5
  permissions: auto  # auto (skip all) | default (allow skip)
  # system_prompts:
  #   - prompts/base.md
  # skills:
  #   - vault
  #   - memory

profiles:
  default:
    model: claude-opus-4-5
    permissions: auto

  # research:
  #   model: claude-opus-4-6
  #   permissions: default
  #   system_prompts:
  #     - prompts/research.md
  #
  # quick:
  #   model: claude-haiku-4-5-20251001
  #   permissions: auto
