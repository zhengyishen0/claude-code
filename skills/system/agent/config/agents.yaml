# Agent settings
# Usage: agent -P <name> "prompt"
# No -P flag uses 'default'

settings:
  default:
    model: claude-opus-4-5
    permissions: auto
    workspace: true
    skills: [all]

  custom:
    model: claude-opus-4-5
    permissions: default
    workspace: true
    # skills: [all]
